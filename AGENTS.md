# 项目协作说明

所有回复和项目文档使用中文。

开始任何工作前，先阅读：

1. `README.md`：项目能力、安装和基础使用。
2. `docs/OPERATIONS.md`：当前部署状态、日常启动、重启、加群、验证和排障。
3. `docs/PERSONA.md`：铃铃酱当前人设的非敏感副本。

## 当前状态

截至 2026-07-20，本项目已经完成并通过实机验收：

- 使用普通 QQ 专用小号，通过 NapCat/OneBot 11 接入，不使用 QQ 开放平台群机器人。
- 当前推荐部署已切换为 Docker Compose：`lingling-bot-core` 与
  `lingling-bot-napcat` 独立运行，NapCat WebUI 为本机 `17099`；macOS 原生
  NapCat 双启动脚本只作为备用方案保留。
- 麦麦与铃铃酱可在本项目中使用 `pnpm bots:start/status/stop` 一次管理；两套
  Compose 使用不同容器、网络、账号和端口。
- 两个项目的共同父目录是 `/Users/why/code/my-project/qq-bots/`；日常优先在父目录
  使用 `pnpm start/status/restart:core/stop` 和 `pnpm bots:login`。`restart:core` 只重建
  两个业务核心，不动 NapCat 和 QQ 登录进程；整套 `pnpm restart` 仅在 NapCat 也需要
  重建时使用。`pnpm login` 属于 pnpm 内置命令，不是机器人登录命令。
- AI 已切换为本机 Codex CLI，默认模型为 `gpt-5.6-luna`、推理深度为 `medium`；文字、实时搜索、识图和图片生成/编辑全部走 Codex。
- 白名单好友私聊文字、图片均可正常回复。
- 专用小号已经加入“杀鸡练习生测试”群，该群已进入白名单。
- 群聊中成员 `@铃铃酱` 时会回复原消息并 `@` 提问者；未 `@` 的普通聊天会进入
  群聊参与模式，经过消息门槛、概率、冷却和 Codex 潜水判断后偶尔自然接话。
- 白名单群已加入主动互动组合：无人回答救场、冷场续聊、每日 `08:00` 成都天气与
  AI/二游/时政/热点新闻情报雷达及 1 张 AI 主题配图、每日 `21:00` 单人单条批斗
  吐槽、旧梗回旋镖和 QQ 消息表情轻回应；
  主动文字每群每天最多 4 条。
- 显式配置的主号可随时私聊发送 `/延年益寿 + 图片` 主动投稿，不再有 `21:50`
  征集提醒。所有原图按计划发布日期归档到
  `/Users/why/Pictures/daily-sese/YYYY-MM-DD/` 并立即预审，只有通过图片进入同目录的
  持久待发布清单；`22:00` 从目录读取后复审，由 Codex 从宽判断是否适合普通群聊，
  通过的投稿会配上一条俏皮但不露骨的文案发到显式目标群。无投稿或全部拒绝时跳过，重启
  不会丢失待发布清单。
- 三个固定任务的实际消息必须显式带 `【情报雷达】`、`【批斗大会】` 或
  `【延年益寿】` 标题，代码会在模型漏写时兜底添加。
- 铃铃酱人设已经加载，自我介绍消息已经在目标群发送并由主号 QQ 界面确认。
- Codex 生成图片会作为 OneBot 图片段发回 QQ，不会只回复本机路径。
- 每位好友或“群 + 成员”的内存会话保留最近 20 轮，闲置 24 小时过期；群聊互动
  另按群短期保留最多 30 条，进程重启都会清空正文。
- `data/group-engagement-state.json` 只持久化每日计数、冷却时间、固定任务日标记、
  上次批斗对象、下次热点时间和最近热点摘要，不保存群友聊天正文；批斗候选正文
  只在内存保留到当晚尝试、自然日切换或进程重启。
- 当前代码已通过类型检查、构建，以及获得本机临时端口权限后的 18 个测试文件共
  108 项测试。Docker 核心已用 `restart:core` 实机重建，NapCat 容器保持不变；QQ
  登录失效、3001 尚未监听时，核心会保持运行并后台重连。主动互动和新版延年益寿
  的真实群触发仍需观察。
- 铃铃酱已调整为动态性格：随语境在松弛、吐槽、冷幽默、发癫、兴奋和认真之间
  切换；“哥哥”和高浓度可爱语气只偶尔使用。“喵~”是高频自然口癖，普通回复
  几乎每条至少一次，较长轻松回复可分散使用一至三次；位置可在自然的句中停顿、
  转折、接话处或句尾，不能固定成结尾后缀。只有危机、安全、强烈悲伤、严肃风险
  提示或必须正式克制等明显不适合的场景才省略。
- 动态人设已用玩笑、日常选择和认真汇报问题连续实测；三次回复在句式、长度和
  认真程度上有明显差异，没有机械添加称呼、Emoji 或“喵~”。
- 旧版提高“喵~”频率后曾用四条连续轻松话题实测，四次中两次使用；这只是历史
  结果，不是当前目标。当前目标已提高为普通回复近乎每条使用，仍需观察真实群聊。

这里记录的是历史验收结果。每次接手时仍需先检查当前进程、NapCat 连接和本地配置，不能假设服务一直在线。

## 不可破坏的约束

- 不得把 `.env.local`、QQ 号、群号、OneBot Token 或 Codex 登录信息输出到聊天、日志或文档。
- QQ 聊天侧的 Codex 只能使用网页搜索、理解附加图片和图片生成/编辑；不得打开 Shell、文件修改、应用、插件、电脑控制或外部账号操作能力。
- 遇到不确定或不能按字面确定指代的网络梗、缩写、谐音、圈内称呼和专有词，回复或生图前必须先实时搜索并交叉核验；仍有多义就先追问，本轮不生成，禁止拆字后臆造形象。
- 当前群内“咕咕嘎嘎”默认指《明日方舟：终末地》的企鹅相关网络梗；仍要搜索当前视觉语境，不得按字面生成为格子、鸽子或鸭子的组合。
- 铃铃酱回复必须口语化、短句、有自然情绪起伏；正常回复禁止标题、编号、项目符号
  和“首先/其次/综上所述”式大纲总结。语气应随话题变化，禁止连续套用高浓度
  可爱模板；允许有小偏好和不确定，但不得编造真实经历。
- 不得让 NapCat 登录主 QQ，只能使用专用机器人小号。
- 不得把 OneBot WebSocket 暴露到公网。Docker 部署只在独立容器网络使用
  `napcat:3001`；WebUI 只映射到宿主机 `127.0.0.1:17099`，并启用 Token。
- 不要手工永久修改 QQ 安装包入口。macOS 双启动只能使用 `scripts/macos/` 下的脚本，它们会瞬时注入并自动恢复原版入口。
- 不要误杀普通 QQ、NapCat 启动器或其他名为 `dist/index.js` 的 Node 服务；重启前必须核对 PID 和命令行。
- 群聊必须保持群白名单；直接回复仍要求 `@机器人`，但用户已明确授权普通群消息
  的话题参与和主动互动模式。不得扩大到非白名单群，也不得改成每条消息都回复。
- 早间情报可搜索成都天气、AI、终末地、绝区零、异环、鸣潮、国内外时政和热点
  新闻；检索时必须优先可靠来源并交叉核验，最终群消息不得附 URL、Markdown 链接
  或单独来源列表。严肃时政、灾害、战争和伤亡只做中性事实概述，不玩梗；无可靠
  天气时潜水，不得把传闻当新闻。决定发送时生成 1 张横版早报主题插画，不搬运
  新闻照片、不描绘真实政治人物或伤亡场面；配图失败不得阻断文字早报。批斗必须
  先把当天缓存
  发言按群友归组，根据一位群友全天发言的整体表现直接发一条吐槽，不得按孤立
  单句断章取义，也不得使用主持或流程公告腔；无厘头罪名必须明显是玩笑且锚定其当天真实发言，不得编造真实黑料、违法行为或攻击
  身份、外貌、家庭、健康和隐私。轻量回应必须调用 QQ 消息表情接口，
  不能改成颜文字、表情包或额外文字。
- 加入新群、邀请成员或主动发送群消息属于外部操作，只能在用户明确授权具体目标后执行。
- 延年益寿投稿人和目标群必须显式配置，且分别是私聊白名单和群白名单的子集；
  不得把投稿扩散到其他好友或群。投稿图片必须先经 Codex 逐图审核；普通群聊可以
  分享时原则上通过，不得仅因无法证明具体年龄、非女性、非二次元、单纯校服或萌系
  画风就自动拒绝。必须拒绝明确的真人私密或性化内容、明显未成年角色的性化呈现、
  裸体或关键部位裸露、性行为、强烈恋物特写、胁迫、暴力色情及其他确实不适合普通
  群聊的内容。版权无法自动核验，投稿命令
  的回复必须要求投稿人只提交有权转发的图片。`/取消延年益寿` 只能清空待发送清单，
  不得删除已写入本机日期目录的归档原图。
## 修改与验证要求

- 配置只修改必要字段，不得整份重写 `.env.local`，并保持权限为 `600`。
- 修改铃铃酱人设时，同步更新 `.env.local`、`docs/PERSONA.md`、`src/persona.ts` 和
  `.env.example`；`tests/persona-sync.test.ts` 必须通过，防止各处提示词漂移。
- 修改 TypeScript 代码后依次运行：

```bash
pnpm typecheck
pnpm test
pnpm build
```

- 修改 macOS QQ/NapCat 启动逻辑后还要运行：

```bash
pnpm qq:verify-macos
```

- `pnpm start` 运行的是 `dist/index.js`；macOS 合盖长期运行可用
  `pnpm start:awake`（内部执行 `caffeinate -i pnpm start`）。代码修改后必须先构建
  并重启，不能只改 `src/`。
- 使用 `pnpm status`、`pnpm stop`、`pnpm restart` 管理 Node 机器人；`pnpm stop` 只停止 AI 回复，不停止普通 QQ 或 NapCat 小号。
- 当前生产部署优先使用 `pnpm docker:start/status/logs/login/stop/restart`；需要一起管理
  同级 MaiBot 时使用 `pnpm bots:start/status/stop/restart`。日常重启业务优先使用
  `pnpm docker:restart:core`、`pnpm bots:restart:core` 或父目录的 `pnpm restart:core`，
  避免重建 NapCat。不要同时运行原生 Node 实例和 Docker 核心，否则会重复回复。
- 重启会清空内存中的对话历史，这是当前设计，不是数据丢失故障。

## 代码导航

- `src/config.ts`：环境变量校验、白名单、Codex 与主动互动参数。
- `src/persona.ts`：铃铃酱完整内置默认提示词，必须与环境模板和人设文档一致。
- `src/create-bot.ts`：OneBot 事件入口、限流、队列、图片加载和回复装配。
- `src/engagement-state.ts`：主动文字、轻量表情、固定任务和热点调度的最小持久状态。
- `src/daily-image-archive.ts`：投稿原图的日期目录、连续编号与本机持久化。
- `src/daily-longevity-archive.ts`：待发布清单持久化、按需读取与重启恢复。
- `src/daily-longevity.ts`：随时投稿预审、发布日期选择、22:00 复审与群发编排。
- `src/group-chat-handler.ts`：命令、会话上下文和 AI 对话编排。
- `src/group-participation.ts`：群级短期上下文、话题参与、定时救场、早报、批斗、
  热点、旧梗和轻量表情编排。
- `src/onebot/message.ts`：私聊、群聊、`@`、CQ 码和图片消息解析。
- `src/onebot/image-loader.ts`：QQ 图片读取、大小/格式/来源限制和 data URL 转换。
- `src/ai/codex-cli-ai.ts`：受限 Codex 非交互调用、JSONL 结果解析和生成图片读取。
- `scripts/macos/`：普通 QQ 与 NapCat 小号双启动及恢复保护。
- `Dockerfile`、`compose.yaml`、`scripts/docker/`：铃铃酱完整容器部署及双机器人
  一键管理。

遇到运行问题先复现和读取日志，再按 `docs/OPERATIONS.md` 排查，不要先大范围改代码。
